export enum AppState {
  DEFAULT = 'default',
  ADD_WORDS = 'addWords',
  TRAIN_WORDS = 'trainWords',
}

export enum ADD_WORDS_STAGE {
  DEFAULT = 'default',
  WORD = 'word',
  TRANSLATION = 'translation',
}
